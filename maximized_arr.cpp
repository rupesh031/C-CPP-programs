#include <iostream>
#include <bits/stdc++.h>
int main()
{
    int n;
    int a[n], b[n];
    for (int i = 0; i < n; i++)
        cin >> a[n];
    for (int i = 0; i < n; i++)
        cin >> b[n];
    int k = 0;

    int c[n];
    for (int i = 0; i < 2 * n; i++)
    {
        c[i] = a[i];
    }
}